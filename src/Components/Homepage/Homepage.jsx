
import React from 'react'
import './Homepage.css';

export const Homepage = () => {
  return (
    <div>
        <nav className="nav">
            
            <ul>
                <a href="#">Home</a>
                <a href="#">About us</a>
                <a href="#">Services</a>
                <a href="#">Contact</a>
                <a href="#">Login</a>
            </ul>
        </nav>
    </div>
  )
}
